{
  "name": "virtual-workspace",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "spike:position": "pnpm --filter scripts spike:position"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
